export const metadata = {
	title: "Calendar | Hero.AI",
	description: "Supercharged Personal AI Assistant",
};

export default function CalenderPage({
	children,
}: {
	children: React.ReactNode;
}) {
	return <>{children}</>;
}
